version: '3.8'

services:
  stonerva_backend:
    build:
      context: .
    container_name: stonerva_backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - app-network
networks:
  app-network:
    driver: bridge
